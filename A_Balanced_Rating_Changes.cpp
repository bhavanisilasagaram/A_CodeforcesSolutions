#include <iostream>
#include <bits/stdc++.h>
#define fastio ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL);
#define ll long long
using namespace std;
int main() {
fastio;
int num;
cin>>num;
int evsum=0;
for(int i=0;i<num;i++){
    int ele;
    cin>>ele;
    if(ele%2==0){
        evsum+=(ele/2);
    }
    else{
        
    }
}

return 0;
}